# System Design Foundations: Чому код — це лише деталь. Mindset, NFR та Артефакти (Extended)

> **English version:** [English](en/_system_design_foundations.md)

**Аудиторія:** Майбутні System Architects / Tech Leads
**Ціль:** Зрозуміти різницю між написанням коду та проєктуванням систем. Навчитися бачити "невидимі" вимоги середовища виконання та оперувати компромісами (Trade-offs).

---

##  1. Експрес-активація (Warm-up)

**Питання 1:** Чим принципово відрізняється робота Senior Developer від Solution Architect?

<details>
<summary>Відповідь</summary>

* **Senior Developer:** Фокус на **локальній оптимізації** (Clean Code, SOLID, алгоритмічна складність  всередині функції).
* *KPI:* Code Coverage, Cyclomatic Complexity.
* *Горизонт планування:* Спринт – Місяць.

* **Architect:** Фокус на **глобальній оптимізації** (TCO — Total Cost of Ownership, MTTD/MTTR, CAP-теорема). Рішення часто субоптимальні для коду, але вигідні для бізнесу.
* *KPI:* Scalability (RPS), Availability (SLA/SLO), Cost Efficiency.
* *Горизонт планування:* Рік – 5 років.
</details>

**Питання 2:** У вас є ідеальний код, покритий тестами на 100%. Але при запуску в Production система падає. Чому?

<details>
<summary>Відповідь</summary>

Проблема в **Runtime Constraints**. Unit-тести живуть у вакуумі.
Production має:
* **Noisy Neighbors:** Сусідні поди в K8s з'їли CPU (Throttling).
* **Network Saturation:** TCP Retransmits через поганий канал.
* **Resource Limits:** OOMKilled через неврахований оверхед JVM/Go Runtime.
* **Connection Exhaustion:** Закінчилися вільні дескриптори або порти в пулі БД.
</details>

**Питання 3:** Якщо AI може згенерувати код за секунди, навіщо нам архітектор?

<details>
<summary>Відповідь</summary>

AI генерує **імплементацію**, а не **стратегію**.
Архітектор — це **Director of Code**, який:

1. Валідує дизайн на відповідність NFR (Non-Functional Requirements).
2. Керує технічним боргом.
3. Приймає рішення в умовах невизначеності (де AI галюцинує).
</details>

---

## 2. Філософія Vibe Coding: Інженер як Директор

Світ змінився. Ми більше не друкуємо код вручну. Ми керуємо його генерацією.
**Vibe Coding** — це не про "розслаблений кодинг", це про зміну ролі інженера з **"Будівельника"** на **"Архітектора/Директора"**.

### Екзоскелет Інженера

* **Old Way (Typing):** Ви думаєте про синтаксис, коми та дужки. Ваша швидкість обмежена швидкістю пальців.
* **New Way (Directing):** Ви думаєте про **Intent (Намір)** та **Constraints (Обмеження)**.

#### Приклад діалогу (Prompt Engineering for Architects):

* *Ви:* "Мені потрібен сервіс для обробки замовлень, який витримає 10k RPS."
* *AI:* Генерує простий REST контролер з записом у БД.
* *Ви (Validation & Hardening):* "Ти використав синхронний запит до бази. При 10k RPS це створить Connection Storm.
1. Перероби на асинхронну модель через Kafka.
2. Додай **Idempotency Key** для захисту від дублів.
3. Реалізуй **Graceful Shutdown**, щоб не губити повідомлення при редеплої."

> **Головне правило Vibe Coding:** Ви не приймаєте код від AI, якщо не можете пояснити, як він поведе себе під навантаженням. AI — це ваш "Draftsman" (кресляр), але підпис під кресленням ставите ви. Ви відповідаєте за **Corner Cases** та **Failure Modes**.

---

## 3. Code vs Execution Environment: Де живе ваша система?

Поширена помилка початківців: вважати, що вимоги приходять лише від Бізнесу (функціонал).
Професійний Архітектор знає: **50% вимог диктує Середовище Виконання (Runtime).**

### "Прихований" стек вимог (Runtime Constraints)

1. **Infrastructure Leakage (Протікання інфраструктури):**
У сучасному Cloud-Native світі абстракції протікають.
* *Приклад:* `Retry` логіка.
* *Bad Practice:* Hardcoding Retry policies in App: Налаштовувати ретраї всередині коду (через бібліотеки типу Resilience4j або конфіги HTTP-клієнта), що створює розсинхрон налаштувань між різними сервісами.
* *Good Practice:* Делегувати це Sidecar-у (Envoy/Istio) з налаштованим **Exponential Backoff + Jitter**, щоб запобігти ефекту "Thundering Herd" (ампліфікації навантаження, коли всі клієнти одночасно повторюють запит і добивають сервіс).

2. **Maintenance Reality (Реальність обслуговування):**
Системи не живуть вічно без втручання.
* *Сценарій:* У вашого Load Balancer закінчився SSL-сертифікат або треба оновити схему БД.
* *Pattern:* **Zero Downtime Deployment**.
* База даних: Патерн "Expand and Contract" (додати колонку -> залити код -> мігрувати дані -> видалити стару колонку).
* Сервіс: Readiness/Liveness probes для коректного перемикання трафіку.

3. **Deployment Safety:**
Ви не можете просто "залити" новий код.
* *Вимога:* Архітектура повинна підтримувати **Observability First**.
* Код має експортувати метрики (Prometheus) та трейси (OpenTelemetry) *до* того, як піде в прод. Якщо ви не бачите `latency_bucket`, ви сліпі.

### 🛑 Помилкові припущення (Fallacies of Distributed Computing) — Deep Dive

Архітектори-початківці часто проектують системи в ідеальному світі. Реальний світ жорстокий.

1. **Мережа надійна.**
* *Реальність:* TCP timeout, DNS failures, BGP rerouting.
* *Рішення:* Idempotency (повтор безпечний), Dead Letter Queues (DLQ).

2. **Latency дорівнює нулю.**
* *Реальність:* Запит Київ-Нью-Йорк — це ~120мс (фізика світла). N+1 запитів до бази через океан вб'ють UX.
* *Рішення:* Data Locality, Caching (CDN, Redis), Batch requests.

3. **Пропускна здатність (Bandwidth) нескінченна.**
* *Реальність:* 1Gbps канал легко забити нестиснутим JSON або логами.
* *Рішення:* Protobuf/gRPC (бінарні формати), Compression (Gzip/Snappy), Pagination.

4. **Мережа безпечна.**
* *Реальність:* Sniffing, MITM.
* *Рішення:* mTLS (Mutual TLS), Zero Trust Architecture.

5. **Топологія не змінюється.**
* *Реальність:* Pods in K8s are ephemeral (pets vs cattle). IP змінюються щохвилини.
* *Рішення:* Service Discovery (Consul, K8s DNS), Client-side Load Balancing.

6. **Транспортна ціна дорівнює нулю.**
* *Реальність:* Серіалізація/десеріалізація (Marshalling) коштує CPU.
* *Рішення:* Efficient schemas, CPU profiling (Flamegraphs).

---

## 4. Методології дизайну: Як почати проект?

Коли ви бачите "чистий аркуш", виникає паніка. Як перетворити ідею на архітектуру? Є два фундаментальні підходи.

##  Top-Down (Згори донизу) — Domain Driven Focus

* **Філософія:** "Від потреби користувача до заліза".
* **Процес:**
1. **User Needs & Ubiquitous Language:** Спілкування з бізнесом, виділення Bounded Contexts (Обмежених контекстів).
2. **API Design (Contract First):** OpenAPI (Swagger). Спочатку узгоджуємо інтерфейс, потім кодимо.
3. **High-Level Components:** C4 Model (Context -> Container -> Component).
4. **Database & Infra:** Вибір CAP-tradeoffs (Consistency vs Availability).

* **Коли використовувати:** Greenfield, складні бізнес-домени, де логіка важливіша за перформанс.
* **Vibe Coding Tip:** "Згенеруй PlantUML діаграму Sequence flow для процесу замовлення таксі".

### 🧱 Bottom-Up (Знизу догори) — Data Driven Focus

* **Філософія:** "Від наявних можливостей до нових фіч".
* **Процес:**
1. **Data & Capability:** Аналіз схеми даних, пропускної здатності шини, наявних логів.
2. **Service Layer (Composition):** Патерн "Strangler Fig" (поступове винесення функціоналу з моноліту).
3. **API:** Exposure layer (GraphQL/BFF).
4. **UI:** Відображення агрегованих даних.

* **Коли використовувати:** Legacy Modernization, рефакторинг, Hard Hardware constraints (Embedded).

---

## 5. Карта Архітектора: North, South, East, West

У складних розподілених системах легко загубитися. Архітектори використовують сторони світу для стандартизації потоків даних та вибору протоколів.

### Північ (Northbound): Вхідні двері

Це ваш публічний фасад. Тут середовище вороже (Internet).

* **Клієнти:** SPA, Mobile, IoT devices.
* **Протоколи:** HTTP/2 (REST), GraphQL (для гнучкості UI), WebSockets.
* **Компоненти:**
* **API Gateway (Kong/Nginx):** Маршрутизація, SSL Termination.
* **BFF (Backend for Frontend):** Агрегація даних під конкретний UI.
* **WAF:** Захист від SQL Injection, XSS.


* **Архітектурний фокус:** Security, Rate Limiting (Token Bucket), Caching strategies (ETag, Cache-Control).

### Південь (Southbound): Фундамент і Стан

Це stateful системи. Тут живе "State".

* **Компоненти:**
* **RDBMS (Postgres):** ACID транзакції.
* **NoSQL (Cassandra/DynamoDB):** High write throughput, Eventual Consistency.
* **Message Brokers (Kafka/RabbitMQ):** Асинхронність.

* **Архітектурний фокус:**
* **CAP Theorem:** Що ми жертвуємо при Partitioning? (Consistency чи Availability).
* **Connection Pooling:** PgBouncer (не відкривати сокет на кожен запит).
* **Data Partitioning/Sharding:** Горизонтальне масштабування даних.

### Схід-Захід (East-West): Внутрішня комунікація (Data Center)

Це трафік між мікросервісами всередині VPC/Cluster.

* **Вимоги:** High Throughput, Low Latency.
* **Протоколи:** gRPC (Protobuf) — швидше за JSON, Thrift.
* **Компоненти:** Service Mesh (Istio/Linkerd).
* **Архітектурний фокус:**
* **Resiliency Patterns:** Circuit Breaker (Hystrix/Resilience4j) — "не стукай у мертвий сервіс".
* **Bulkhead:** Ізоляція пулів потоків (щоб падіння сервісу А не поклало сервіс Б).
* **Distributed Tracing:** Прокидання `TraceID` через усі виклики.

---

## 6. Teamwork & Engineering Friction: Як не вбити один одного?

Архітектура — це **спосіб вирішення конфліктів** (Friction) у команді та фіксація компромісів.

### Артефакти Архітектора (За що вам платять?)

> **Золоте правило:** Якщо рішення не задокументоване, його не існує.

#### 1. Architecture Blueprint (High-Level Design)

"Конституція" проєкту.

* **Tech Stack:** Java/Go, Postgres/Mongo.
* **NFR Values:** RTO/RPO (Recovery Time/Point Objective) — скільки даних ми можемо втратити при аварії?

#### 2. ADR (Architecture Decision Records) — Critical!

Це журнал бортового лікаря.

* **Формат:**
* **Context:** Потрібно обрати брокер повідомлень.
* **Options:** Kafka vs RabbitMQ.
* **Decision:** Обираємо Kafka.
* **Consequences (Trade-offs):** Отримуємо високу пропускну здатність, АЛЕ ускладнюємо інфраструктуру та втрачаємо гнучкий роутинг RabbitMQ.

* *Чому це важливо:* Через рік ніхто не згадає, чому обрали Kafka. ADR захищає від "ефекту нового менеджера".

#### 3. Impact Analysis Document

* **Matrix of Dependencies:** Таблиця "Service A залежить від API v1 Service B".
* **Migration Strategy:** Blue-Green, Canary або Big Bang (тільки якщо не можна інакше).

#### 4. Rollout Checklist (Runbook)

* Pre-deployment: Schema migration (`alembic upgrade head`).
* Verification: `curl /health` повертає 200.
* Rollback: `helm rollback`.

---

## 7. Візуалізація: Як ми "продаємо" рішення?

Архітектор повинен вміти говорити з різними аудиторіями. C4 Model (Context, Containers, Components, Code) — це стандарт індустрії.

### Рівень А: Logical Design (Концептуальний) — "What"

* **Аудиторія:** Бізнес, PM.
* **Мета:** Показати **Value Streams**.
* **Приклад:** Клієнт -> Замовлення -> Оплата -> Доставка. Жодних серверів, лише бізнес-сутності.

### Рівень Б: Physical Design (Інфраструктурний) — "Where"

* **Аудиторія:** DevOps, SRE.
* **Мета:** Топологія розгортання.
* **Приклад:**
* "Замовлення" -> K8s Deployment (3 replicas), Requests: 200m CPU.
* "Оплата" -> External API Stripe.
* "Зв'язок" -> Ingress Controller, TLS v1.3.


---

## 8. Ціна архітектурної помилки: Post-Mortem Analysis

Архітектор — це людина, яка приймає **найдорожчі рішення** до того, як написано перший рядок коду. Помилка на етапі дизайну коштує в 100 разів дорожче, ніж баг у коді.

##  Кейс 1: Ефект "Ticketmaster" (Distributed Locking fail)

* **Проблема:** При продажу квитків тисячі користувачів намагалися купити *одне й те саме* місце одночасно.
* **Технічна причина:** Використання optimistic locking (рівень БД) замість розподіленого кешу (Redis/Lua scripts) для резервації слотів. База "лягла" від кількості `ROLLBACK` транзакцій.
* **Архітектурний урок:** Для High Contention ресурсів (квитки, складські залишки) потрібна черга або in-memory серіалізація запитів, а не прямий запис у БД.

##  Кейс 2: Mars Climate Orbiter ($327 млн) — Integration Hell

* **Суть:** Метрична система vs Імперська.
* **Архітектурний урок:** Відсутність **Contract Testing** (Pact) та типізації даних на рівні API схем. Інтерфейси повинні бути суворо типізовані (Strongly Typed IDL).

##  Кейс 3: Cascading Failures (Ефект доміно)

* **Сценарій:** Впав сервіс рекомендацій. Головна сторінка магазину теж перестала вантажитися.
* **Причина:** Відсутність **Timeouts** та жорстка залежність (Strong Coupling). Фронтенд чекав відповіді нескінченно.
* **Архітектурний урок:** **Graceful Degradation**. Якщо рекомендації недоступні — показуй "Топ продажів" або просто порожній блок, але дай користувачеві купити товар.

---

##  Питання до групи (Discussion)

*Ситуація: Ви будуєте систему для Tier 1 Telecom оператора. Вам потрібно додати аналітику.*
**Питання:** До якої сторони світу (N/S/E/W) відноситься інтеграція з зовнішньою Google Analytics? А з внутрішнім Data Warehouse на Hadoop?

<details>
<summary>Відповідь</summary>

* **Google Analytics:** Client-side integration. З точки зору бекенду це невидимо. Якщо Server-side tracking — це **Southbound** (зовнішній Sink, куди ми "зливаємо" дані без очікування відповіді).
* **Internal Data Warehouse:** **Southbound** (якщо ми пишемо в HDFS) або **East-West** (якщо ETL процес витягує дані з нашої Kafka). Важливо тут: ETL не повинен "просадити" операційну базу (Isolation of Workloads).

</details>

---

##  9. Фінальні контрольні питання (Checklist)

1. **Про NFR:** Чому "швидко" — це погана вимога? Як архітектор має сформулювати вимогу до швидкодії?
<details>
<summary>Відповідь</summary>

"Швидко" не можна поміряти. Архітектор каже: "P99 Latency < 200ms при 10k RPS". Це означає, що 99% запитів будуть швидші за 200мс навіть під навантаженням.
</details>

2. **Про Артефакти:** Ви змінюєте структуру бази даних білінгу. Який документ ви маєте підготувати першим?
<details>
<summary>Відповідь</summary>

**Impact Analysis** (кого зачепить) та **ADR** (чому ми це робимо і як мігруємо).
</details>

3. **Про Сторони світу:** Де в архітектурній діаграмі місце для Load Balancer?
<details>
<summary>Відповідь</summary>

**Northbound**. Це щит системи.
</details>

4. **Про Хибні Припущення:** Ваша команда каже: "Ми не будемо обробляти таймаути мережі, бо у нас надійний дата-центр". Яку помилку вони роблять?

<details>
<summary>Відповідь</summary>
Пастка "Network is reliable". Потрібно впроваджувати **Retry (з Jitter)** та **Circuit Breaker**.
</details>
