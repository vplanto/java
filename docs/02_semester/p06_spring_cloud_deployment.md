# Практикум 5: Hello Cloud. Деплоймент у Інтернет

**Тип:** Hands-on Lab
**Рівень:** Junior Strong
**Попередні вимоги:** Робочий сервіс у Git-репозиторії (GitHub/GitLab).
**Інструменти:** Render.com (Free Tier), Git.
**Інженерна мета:** Вийти за межі `localhost`. Зрозуміти концепцію PaaS (Platform as a Service) та CI/CD.

---

##  Експрес-опитування: Localhost vs Production

1.  **Availability:** Ви закрили кришку ноутбука. Що сталося з вашим сервером на `localhost:8080`? Чи доступний він клієнтам?
2.  **Access:** Ваш друг з іншого міста хоче перевірити API. Чи спрацює у нього посилання `http://192.168.0.105:8080`?
3.  **Environment:** У вас Java 21, а на сервері Java 17. Що станеться при запуску?

<details markdown="1">
<summary>Відповіді (Самоперевірка)</summary>

1.  **Він "помер".** Сервер працює, поки працює процес ОС. Немає живлення — немає сервісу. Cloud-провайдери гарантують 24/7 (SLA).
2.  **Ні.** Це ваша локальна IP-адреса всередині квартири (NAT). Щоб вас побачив світ, потрібна "Публічна IP" (Public IP) або доменне ім'я.
3.  **Crash.** `UnsupportedClassVersionError`. Це класична проблема "Works on my machine". Тому ми використовуємо Docker або чітко налаштовані PaaS-середовища.

</details>

---

## 🛑 Engineering Context: The Cloud & PaaS

Раніше інженери купували фізичні сервери, встановлювали Linux, налаштували мережу, встановлювали Java, відкривали порти... Це **IaaS (Infrastructure as a Service)**.

Сьогодні ми використовуємо **PaaS (Platform as a Service)**.
Ви кажете хмарі: *"Ось мій код на Java"*.
Хмара відповідає: *"Я сама знайду вільний сервер, встановлю Linux, скомпілюю твій код, запущу його і дам тобі посилання HTTPS"*.

Ми використаємо **Render.com** — сучасну хмарну платформу.

---

## Крок 1. Git Push (Якщо ще не зробили)

Хмара не бачить файлів на вашому диску. Вона бачить ваш Git.

1.  Створіть репозиторій на GitHub.
2.  Виконайте в папці проєкту:
    ```bash
    git init
    git add .
    git commit -m "Initial commit for deployment"
    git branch -M main
    git remote add origin <ВАШЕ_ПОСИЛАННЯ_НА_REPO>
    git push -u origin main
    ```

> **Важливо:** Перевірте, що файл `pom.xml` лежить у корені репозиторію.

---

## Крок 2. Налаштування Render.com

1.  Зареєструйтесь на [dashboard.render.com](https://dashboard.render.com/) (можна через GitHub акаунт).
2.  Натисніть **New +** -> **Web Service**.
3.  Виберіть **Build and deploy from a Git repository**.
4.  Знайдіть свій репозиторій у списку і натисніть **Connect**.

---

## Крок 3. Конфігурація середовища (Environment)

Render запитає, як запускати ваш додаток. Заповніть уважно:

* **Name:** `demo-service-student-name` (буде частиною URL).
* **Region:** Frankfurt (EU Central) — найближчий до нас.
* **Branch:** `main`.
* **Runtime:** **Java**.
* **Build Command:** `mvn clean package -DskipTests`
    *(Ми пропускаємо тести для економії часу на безкоштовному тарифі, але в продакшені так робити не можна!)*.
* **Start Command:** `java -jar target/demo-service-0.0.1-SNAPSHOT.jar`
    *(Перевірте версію та назву в pom.xml, якщо змінювали)*.
* **Instance Type:** **Free**.

---

## Крок 4. Змінні середовища (Environment Variables)

Пам'ятаєте Лаб 4? Ми винесли конфігурацію. Тепер ми можемо змінити поведінку програми без зміни коду.

1.  Прокрутіть до секції **Environment Variables**.
2.  Додайте змінну:
    * **Key:** `APP_CURRENCY_USD_RATE` (або як ви назвали її в `@Value`, Spring автоматично мапить `app.currency.usd-rate` -> `APP_CURRENCY_USD_RATE`).
    * **Value:** `40.5` (Встановимо інший курс, ніж локально).
3.  Натисніть **Create Web Service**.

---

## Крок 5. Build & Deploy

Дивіться в консоль (Logs) прямо в браузері.
1.  Render клонує репозиторій.
2.  Завантажує Maven-залежності (це займе 2-3 хвилини перший раз).
3.  Компілює Jar.
4.  Пише: `Build successful`.
5.  Запускає команду Java.
6.  Чекаємо напис: `Tomcat started on port 8080`.
7.  Статус зміниться на **Live** 🟢.

Угорі сторінки з'явиться посилання типу: `https://demo-service-xyz.onrender.com`.

---

## Крок 6. Тестування

Відкрийте термінал на комп'ютері (або Postman). Ви більше не звертаєтесь до `localhost`.

**Запит:**
```bash
curl "https://<ВАШ_URL>[.onrender.com/api/currency/convert-details?amount=100](https://.onrender.com/api/currency/convert-details?amount=100)"

```

**Очікуваний результат:**
JSON з розрахунком по курсу **40.5** (який ви задали в змінних середовища), а не 38.5 (який був у коді).

---

##  Engineering Case Study: "Cold Start"
Ви залишили сервіс на годину. Потім намагаєтесь відкрити посилання, і воно вантажиться 30-50 секунд. Чому?

<details markdown="1">
<summary>Архітектурне рішення</summary>

Це особливість **Free Tier**.
Хмара економить ресурси. Якщо до вашого сервісу немає запитів певний час, Render "присипляє" його (Scale to Zero).
Перший запит "будить" процес: виділяється RAM, запускається JVM, стартує Spring Boot. Це називається **Cold Start**.
У платному Production цього не відбувається (там завжди є "гарячий" інстанс).

</details>

---

##  Завдання "На захист" (Continuous Deployment)
1. Змініть у коді `GreetingController` повідомлення на "Hello from Cloud!".
2. Зробіть `git commit` та `git push`.
3. Нічого не чіпайте в Render. Просто відкрийте Dashboard.

**Питання:** Що відбулося автоматично і чому це круто для бізнесу?

<details markdown="1">
<summary>Відповідь</summary>

Render налаштований на **Auto-Deploy**.
Він побачив зміни в гілці `main`, сам запустив новий білд і замінив стару версію програми на нову без вашої участі.
Це основа **CI/CD (Continuous Integration / Continuous Deployment)**. Це зменшує "Time-to-Market" доставки фіч клієнтам.

</details>

---

##  Контрольні питання
1. **Logs:** Де тепер шукати помилки (`NullPointerException`), якщо консоль IntelliJ IDEA не показує логи сервера?
2. **Filesystem:** Ваша програма зберігає завантажені картинки у папку `images/` поруч із `jar` файлом. Після перезавантаження (Deploy) картинки зникли. Чому?
3. **Port:** У коді `application.properties` написано `server.port=8080`. Але хмара видала вам HTTPS (443). Як це працює?

<details markdown="1">
<summary>Відповіді</summary>

1. **У дашборді провайдера (Logs tab).** Або підключати централізовані системи логування (ELK, Datadog), куди сервер буде відсилати логи.
2. **Ephemeral Filesystem (Ефемерна файлова система).** У хмарі (особливо Docker/PaaS) диск очищується при кожному перезапуску. Файли треба зберігати у зовнішніх сховищах (AWS S3, Google Storage), а не на диску сервера.
3. **Reverse Proxy.** Ваш Java-додаток дійсно слухає порт 8080 всередині контейнера. Але перед ним стоїть Load Balancer хмари, який приймає HTTPS (443) і перенаправляє трафік на ваш внутрішній 8080.

</details>
---

**[⬅️ P05: Production Ready](p05_spring_production_ready.md)**

**[⬅️ Повернутися до головного меню курсу](index.md)**
